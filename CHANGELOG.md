# سجل التعديلات والتحديثات (CHANGELOG)

يقوم هذا الملف بتسجيل وتوثيق كافة التعديلات والتحسينات المنجزة على مشروع "أوقاف 15 مايو" باليوم، الوقت والتفاصيل الشاملة.

---

## [2026-07-26 00:36] — إصلاح انقطاع القائمة المنسدلة في أسفل الجدول وتنسيق خيارات المحجوزين

### 1. منع انقطاع واختفاء القائمة المنسدلة في أسفل الجدول (`dropdownParent: 'body'`)
- **الملفات المعدلة**: `schedule.html` | `assets/style.css`
- **التفاصيل**:
  - تم تفعيل خيار `dropdownParent: 'body'` بداخل مكتبة TomSelect ليتم ربط القائمة المنسدلة مباشرة بـ `body` بدلاً من حافات الجدول.
  - أصبحت القائمة تطفو أعلى كل العناصر (`z-index: 99999`) وتنفتح للأعلى تلقائياً عند اختيار مساجد أسفل الصفحة بدون أن تقص أو تختفي خلف حدود الحاوية.

---

### 2. التنسيق البارز لوسوم المحجوزين بالأحمر (`⚠️ مسند إلى: اسم المسجد`)
- **الملفات المعدلة**: `schedule.html` | `assets/style.css`
- **التفاصيل**:
  - تصحيح دالة المطابقة لقوائم المحجوزين وإبراز الشارة الحمراء بخلفية وردية وحدود واضحة: `[⚠️ مسند إلى: اسم المسجد]`.
  - إظهار خيارات المجموعة الرابعة بوضوح مع إمكانية التمرير التلقائي الكامل بداخل القائمة المنسدلة.

---

## [2026-07-26 00:32] — تطوير ترتيب ونمط خيارات اختيار الخطيب والتعامل الذكي مع المحجوزين

### 1. إظهار الخطباء المقترحين للمسجد وترتيب المجموعات
- **الملفات المعدلة**: `schedule.html`
- **التفاصيل**:
  - تم إعادة ترتيب خيارات الخطباء بداخل القائمة المنسدلة (TomSelect) لكل مسجد كالتالي:
    1. **المجموعة الأولى**: الخطيب الثابت للمسجد (يميز بنجمة ذهبية `★`).
    2. **المجموعة الثانية**: الخطباء الذين اختاروا هذا المسجد كـ (رغبة / مقترح) في التفضيلات (يميزون بشارة زرقاء `[مقترح لهذا المسجد]`).
    3. **المجموعة الثالثة**: باقي الخطباء المتاحين في هذه الجمعة.
    4. **المجموعة الرابعة والأخيرة**: الخطباء المسندون إلى مسجد آخر في نفس الجمعة.

---

### 2. تمييز الخطباء المحجوزين بالأحمر وإتاحة نقلهم التلقائي
- **الملفات المعدلة**: `schedule.html`
- **التفاصيل**:
  - يظهر أمام الخطيب المحجوز في مسجد آخر شارة حمراء بارزة: `[⚠️ مسند إلى: اسم المسجد الآخر]`.
  - أصبح بالإمكان اختيار الخطيب المحجوز عادي جداً؛ وعند اختياره يقوم النظام تلقائياً بـ:
    1. إزالته وإلغاء إسناده من المسجد السابق وتفريغ ذلك المسجد (ليصبح `شاغر`).
    2. إسناده للمسجد الجديد، وتحديث الصفين بتمييز التعديلات وتفعيل زر الحفظ تلقائياً.

---

## [2026-07-26 00:28] — رفع المشروع على GitHub وتفعيل النشر (GitHub Pages)

### 1. إنشاء المستودع المباشر ورفع الكود
- **التفاصيل**:
  - إنشاء المستودع العام `khotba-web` على حساب GitHub للمستخدم (`omarmagdy99`).
  - رفع كافة التحديثات والملفات وإصدار السجل إلى الفرع الرئيسي `main`.
  - رابط المستودع: `https://github.com/omarmagdy99/khotba-web`

---

### 2. تفعيل خدمة النشر المباشر (GitHub Pages)
- **التفاصيل**:
  - تفعيل النشر التلقائي عبر **GitHub Pages** على فرع `main`.
  - رابط النشر الحي العام للمشروع: `https://omarmagdy99.github.io/khotba-web/`

---

## [2026-07-26 00:25] — حفظ وتأكيد كافة التعديلات في Git (Commit)

### 1. إتمام الـ Git Commit المحلي
- **التفاصيل**:
  - تم ربط وتوثيق كافة التعديلات على ملفات الواجهة والسكربتات والأنساق وإضافة سجل `CHANGELOG.md` بداخل الـ Git Repository المحلي للمشروع بحساب المستخدم (`Omar Magdy`).
  - كود التغيير الـ Commit Hash: `060c26e`.

---

## [2026-07-26 00:21] — إلغاء التوقف المتسلسل وتسريع فتح شاشة التوزيع والجمعات فورياً

### 1. تسريع التحميل الأولي للواجهة (Parallel Auth & Data Loading)
- **الملفات المعدلة**: `schedule.html` | `assets/app.js`
- **التفاصيل**:
  - دمج كافة طلبات فتح الصفحة (`requireAuth`, `loadReference`, `getSchedule`, `refreshLoadCounts`) في دالة متوازية واحدة (`Promise.all`) بدلاً من الانتظار التتابعي التسلسلي الذي كان يسبب ظهور مؤشري التحميل المتعاقبين (`جاري التحميل...` ثم `جاري تحميل التوزيع...`).
  - النتيجة: اختفاء مؤشر التحميل الرئيسي في أسرع وقت ممكن وفتح الشاشة فورياً.

---

### 2. التنقل الفوري بين الجمعات بدون انتظار (0ms Friday Switching)
- **الملفات المعدلة**: `assets/app.js` | `schedule.html`
- **التفاصيل**:
  - إدراج طلبات جدول الجمعة (`getSchedule`) وعدد الخطب (`getLoadCounts`) بداخل كاش المتصفح المباشر (`sessionStorage`).
  - عند التنقل بين الجمعات المتاحة أو الضغط على أزرار التواريخ (Chips)، تعرض الجمعة المحددة بـ **0 مللي ثانية (فورياً بدون أي دايرة تحميل)** إذا كانت محملة سابقاً.

---

## [2026-07-26 00:16] — تفعيل كاش المتصفح والسيرفر (Double Caching) لتسريع الأداء 10 أضعاف

### 1. الكاش الذكي بداخل المتصفح (Client-side sessionStorage)
- **الملفات المعدلة**: `assets/app.js`
- **التفاصيل**:
  - إضافة ذاكرة مؤقتة لـ `sessionStorage` بداخل المتصفح لقوائم `listKhatibs` و `listMosques` و `getSettings`.
  - أصبح الانتقال والتصفح بين التابات والصفحات يتم في غضون **0 مللي ثانية (فوري)** بدون أي انتظار لاستجابة السيرفر.
  - تفريغ الكاش المحدث تلقائياً (`clearClientCache`) فور قيام المستخدم بإضافة أو تعديل أو إلغاء تنشيط خطيب أو مسجد أو تغيير التفضيلات والنشر، لضمان دقة البيانات دائماً.

---

### 2. الكاش السريع بالسيرفر (`CacheService` في Apps Script)
- **الملفات المعدلة**: `apps-script/Code.gs`
- **التفاصيل**:
  - إضافة `CacheService.getScriptCache()` لحفظ البيانات المرجعية للسيرفر لمدة 10 دقائق لتجنب القراءة المكررة لصفوف ملف Google Sheets.
  - تقليل وقت استجابة Apps Script عند الطلبات المباشرة من ~1.8 ثانية إلى ~0.3 ثانية.
  - مسح الكاش السيرفري تلقائياً (`invalidateServerCache_`) مع أي تعديل أو إضافة لضمان التزامن الكامل.

---

## [2026-07-26 00:13] — إضافة رابط النشر المباشر مع زر النسخ إلى الحافظة

### 1. إظهار رابط النشر وخيار النسخ في شاشة النشر
- **الملفات المعدلة**: `publish.html`
- **التفاصيل**:
  - تحديث بانر حالة النشر بداخل صفحة `publish.html` ليعرض رابط النشر العام المباشر للمستخدم فور إتمام عملية النشر.
  - إضافة زر خيار "📋 نسخ رابط النشر" والذي يقوم بنسخ رابط `public.html` مباشرة إلى حافظة جهاز المستخدم مع إظهار إشعار تأكيدي (Toast notification).
  - دعم الدعم الاحتياطي (Fallback clipboard) لضمان التوافقية الكاملة مع جميع المتصفحات والهواتف.

---

## [2026-07-25 23:58] — إصلاح تعارض النوافذ المنبثقة (Modals) وتنسيق القوائم المنسدلة RTL

### 1. حل مشكلة زري "تعديل" و "التفضيلات" والنوافذ المنبثقة
- **الملفات المعدلة**: `assets/style.css`
- **التفاصيل**:
  - معالجة التعارض النمطي (CSS Class Collision) بين كلاسات Bootstrap 5 `.modal` و `.modal-backdrop` وبين النوافذ المنبثقة الخاصة بالنظام.
  - إجبار الفتح والعرض الصريح من خلال تعيين `z-index: 1055/1060` و `display: flex !important` عند تفعيل الخيار `.show`.
  - أصبح أزرار "تعديل" و "التفضيلات" و "إضافة خطيب" و "إضافة مسجد" تعمل فوراً وتظهر النوافذ المنبثقة بشكل ممتاز.

---

### 2. معالجة وتنسيق ديزاين قائمة اختيار الخطيب في RTL
- **الملفات المعدلة**: `schedule.html` | `mosques.html` | `khatib-schedule.html` | `assets/style.css`
- **التفاصيل**:
  - إصلاح تداخل نص اسم الخطيب مع المؤشر أو سهم القائمة المنسدلة عبر توجيه اتجاه الإدخال إلى `direction: 'rtl'`.
  - إضافة `controlInput: null` ونقل خانة البحث الفوري إلى رأس القائمة المفتوحة (`plugins: ['dropdown_input']`) لضمان ظهور اسم الخطيب المحدد بوضوح ونقاء شديد بدون أي رموش إدخال أو حروف متداخلة فوق الاسم.
  - ضبط الحواشي والهوامش في `.ts-control` و `.ts-dropdown` لتلائم النصوص العربية وتظهر بمظهر جذاب ومحاذي للجهات الرسمية.

---

## [2026-07-25 23:53] — تحديث التصميم والبحث والتجاوب والأداء

### 1. دمج Bootstrap 5 RTL على مستوى المشروع بالكامل
- **الملفات المعدلة**: `index.html` | `schedule.html` | `khatibs.html` | `mosques.html` | `khatib-schedule.html` | `publish.html` | `public.html` | `assets/style.css`
- **التفاصيل**:
  - تضمين مكتبة Bootstrap 5.3.3 RTL (CSS & JS) في جميع صفحات النظام المذكورة.
  - تحسين مظهر الواجهات والشبكات النمطية والكروت (Cards & Containers) لضمان تجربة تصفح سلسة ومنظمة، مع التوافقية والتجاوب التام 100% مع الهواتف الذكية والأجهزة المختلفة.
  - تحديث وتخصيص العناصر الرئسية للحفاظ على هوية وألوان النظام (الكحلي `#1e3c5c` مع خلفيات `#f0f4f8`).

---

### 2. إضافة القوائم القابلة للبحث (Searchable Selects) عبر مكتبة Tom-Select
- **الملفات المعدلة**: `schedule.html` | `mosques.html` | `khatib-schedule.html` | `assets/style.css`
- **التفاصيل**:
  - دمج مكتبة **Tom-Select** الحديثة والخفيفة المعتمدة على Bootstrap 5 في قائمة اختيار الخطباء.
  - إتاحة خيار **البحث الفوري بالإدخال والتصفية** داخل قوائم التوزيع وقوائم اختيار الخطيب الثابت بدلاً من القوائم التقليدية غير المنظمة.
  - تخصيص تصميم القائمة المنسدلة والرموز والحواشي الدائرية لتلائم مظهر التطبيق بشكل رائع وجذاب.

---

### 3. مراجعة وتحسين أداء المشروع (Performance Optimization)
- **الملفات المعدلة**: `schedule.html` | `khatibs.html` | `mosques.html`
- **التفاصيل**:
  - **التزامن والتحميل المتوازي (`Promise.all`)**: تم تحسين دوال `loadReference()` و `loadData()` في الصفحات الرئيسية لاستدعاء بيانات الخطباء والمساجد بشكل متوازٍ في نفس الوقت بدلاً من الانتظار التتابعي التسلسلي، مما قلل وقت التحميل الأولي بنسبة تصل إلى 50%.
  - **تحسين تحديث الجدول (DOM Rendering Optimization)**: في صفحة التوزيع (`schedule.html`)، تم تحسين آلية اختيار الخطيب لتحديث حالة "الحفظ" وعداد التغييرات وتنبيه الصف المعدل تلقائياً دون الحاجة إلى مسح وإعادة رسم جدول الصفحة بالكامل، مما يمنع التجميد المؤقت للشاشة أو فقدان التركيز (Focus) أثناء التوزيع.

---

### 4. إنشاء ملف التوثيق السجلي `CHANGELOG.md`
- **الملف المحدث**: `CHANGELOG.md`
- **التفاصيل**:
  - إنشاء هذا الملف بداخل جذر المشروع لتسجيل واستكمال كافة التعديلات المستقبلية بالتاريخ والوقت والتوضيح التفصيلي.
